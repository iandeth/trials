<%= form_for(@post_label) do |f| %>
  <% if @post_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_label.errors.count, "error") %> prohibited this post_label from being saved:</h2>

      <ul>
      <% @post_label.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br />
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :label_id %><br />
    <%= f.number_field :label_id %>
  </div>
  <div class="field">
    <%= f.label :memo %><br />
    <%= f.text_area :memo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
